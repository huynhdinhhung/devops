apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gymmanage
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/huynhdinhhung/devops.git
    targetRevision: main
    path: k8s

  destination:
    server: https://kubernetes.default.svc
    namespace: gymmanage

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
